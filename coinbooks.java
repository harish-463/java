import java.util.*;
public class coinbooks {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        int m = scanner.nextInt();
        ArrayList<Integer> books = new ArrayList<>();
        for (int i = 0; i < n; i++) {
            books.add(scanner.nextInt());
        }
        int maxBooks = 0;
        ArrayList<Integer> bookIndices = new ArrayList<>();
        for (int i = 0; i < n; i++) {
            int totalCost = 0;
            ArrayList<Integer> booksBought = new ArrayList<>();

        }
    }
}
